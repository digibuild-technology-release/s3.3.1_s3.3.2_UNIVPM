apiVersion: v1
kind: Service
metadata:
  name: optimization-service
  namespace: my namespace
spec:
  ports:
    - port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app: optimization-api
  type: ClusterIP
